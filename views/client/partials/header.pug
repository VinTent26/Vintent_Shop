include ../mixins/sub-menu.pug
header.header
    .container
        .row.align-items-center
            .col-3
                .inner-logo 
                    a(src="/")
                        img(src="/images/logo.svg", alt="Logo")

            .col-9 
                .inner-menu 
                    ul 
                        li 
                            a(href="/") Trang chủ
                        li(class="sub-menu") 
                            -const patProduct = "/products"
                            a(href=patProduct) Sản phẩm
                                +sub-menu(layoutProductCategory,patProduct)
                            